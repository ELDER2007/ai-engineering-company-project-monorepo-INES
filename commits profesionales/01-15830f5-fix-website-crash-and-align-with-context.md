Fix website crash and align landing/form with CONTEXT.md

Remove stray spread that made options.map fail and blank the page. Move the
talent form to its own /talento page, add a Contacto section, list both
service bullets, restore the full success message, and link form errors to
their fields with aria-describedby.
